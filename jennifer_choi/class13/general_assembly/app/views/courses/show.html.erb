<h1><%= @course.name %></h1>

<h3>Instructor: <%= @instructor.name.capitalize %></h3>
<% @students.each do |s| %>
  <p><%= s.name %></p>
<% end %>

<%= link_to "Create New Student", new_course_student_path(@course) %>